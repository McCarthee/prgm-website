<style is:global>
    :root {
        --primary-color: #ab2330;
        --primary-color-dark: hsl(354, 66%, 30%);
        --secondary-color: #f4f4f4;

        --pdx-container: 4rem;
    }

    body {
        font-family: Raleway, sans-serif;
        background-color: var(--secondary-color);
        min-height: 100vh;
    }

    h1,
    h2,
    h3,
    h4,
    h5,
    h6 {
        font-family: "Bebas Neue", sans-serif;
        color: var(--primary-color);
        line-height: 1;
        margin-bottom: 2rem;
    }

    h1 {
        font-size: 3rem;
    }

    h2 {
        font-size: 2.75rem;
    }

    p {
        font-size: 1.2rem;
        line-height: 1.7;
        margin-bottom: 2.4rem;
        max-width: 60ch;
    }

    .hero * {
        color: var(--secondary-color);
    }
</style>
